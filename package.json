{
  "name": "kagemai",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "kagemai (影舞) — ブラウザだけで動くMRリズムゲーム",
  "scripts": {
    "start": "node scripts/server.mjs --port 4173",
    "test": "node --test \"test/*.test.mjs\"",
    "e2e": "playwright test",
    "test:all": "npm test && npm run e2e"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
